# --- PostgreSQL Configuration ---

# Service host used by the backend (defaults to docker-compose service name)
POSTGRES_HOST=postgres
# PostgreSQL port
POSTGRES_PORT=5432
# Database name
POSTGRES_DB=red_tetris
# Credentials for the database user
POSTGRES_USER=your-username
POSTGRES_PASSWORD=your-strong-password

# Optional: override composed connection string
# POSTGRES_URI=postgresql://your-username:your-strong-password@postgres:5432/red_tetris

# --- Vite Configuration ---

# Set the Socket URL for Vite
VITE_SOCKET_URL=http://HOST_NAME:3000
